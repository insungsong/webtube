extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each video in videos
            +videoBlock({
                title:video.title,
                views:video.views,
                viedeoFile:video.videoFile,
                id:video.id,
                fileUrl:video.fileUrl
            })
            